students : (
    'aiden blishen cuneo',
    'someone else?',
);

capitalise_all : [list,] => {
    n : ();
    func : [a,]=>{ret a.capitalise()};
    for list {
        n +: ' '.join(#.split(' ').map(func));
    }
    ret n;
}

students : capitalise_all(students);
students;
