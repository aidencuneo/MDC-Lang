tag : 10;
tag +: 1;
tag.type : 'TAG';
tag.type +: 10;
!ECHO tag.type;

/*

compile : (data,) => {
    doc : '<html>';
    FOR tag : data : {
        doc : doc + '<' + [tag.name] + '>';
        doc : doc + [tag.inner];
        doc : doc + '</' + [tag.name] + '>';
    };
    YIELD doc + '</html>';
};

div : (inner,) => {
    tag:;
    tag.name  : 'div';
    tag.inner : inner;
    YIELD tag;
};

data : ARRAY;
data : data + [!div 'Hey there'];
html : !compile data;

!ECHO html;
