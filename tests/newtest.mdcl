filter : (! : Array : Function) => {
    f : ARRAY;
    FOR $0 : {
        IF !$1, : {
            f : f + ,;
        };
    };
    YIELD f;
};

list : TO 60;
newlist : !filter list (!:@) => {
    YIELD !BOOLEAN $0;
};

!ECHO list;
!ECHO newlist;
